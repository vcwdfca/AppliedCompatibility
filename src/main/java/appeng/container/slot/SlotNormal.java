package appeng.container.slot;

import net.minecraftforge.items.IItemHandler;

public class SlotNormal extends AppEngSlot {

    public SlotNormal(final IItemHandler inv, final int idx, final int x, final int y) {
        super(inv, idx, x, y);
    }
}
